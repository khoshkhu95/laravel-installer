<?php

return [

    /*
    |--------------------------------------------------------------------------
    | نام نرم افزار جهت نمایش در سصحه خوش آمدگویی
    |--------------------------------------------------------------------------
    */
    'name' => '',
    /*
    |--------------------------------------------------------------------------
    | فایل قفل نصب
    |--------------------------------------------------------------------------
    | نام فایلی که بعد از پایان نصب در storage ساخته می‌شود.
    | وجود این فایل یعنی پروژه نصب شده است.
    */
    'lock_file' => storage_path('installed.lock'),

    /*
    |--------------------------------------------------------------------------
    | پیشوند مسیر (Route Prefix)
    |--------------------------------------------------------------------------
    */
    'route_prefix' => 'installer',

    /*
    |--------------------------------------------------------------------------
    | میدلور مسیرهای نصب‌کننده
    |--------------------------------------------------------------------------
    */
    'route_middleware' => ['web'],

    /*
    |--------------------------------------------------------------------------
    | پیش‌نیازهای PHP (اکستنشن‌ها)
    |--------------------------------------------------------------------------
    */
    'requirements' => [
        'php_version' => '8.1.0',
        'extensions' => [
            'openssl',
            'pdo',
            'mbstring',
            'tokenizer',
            'json',
            'ctype',
            'bcmath',
            'fileinfo',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | پوشه‌ها و فایل‌هایی که باید قابل نوشتن باشند
    |--------------------------------------------------------------------------
    */
    'permissions' => [
        'storage/framework/' => '775',
        'storage/logs/' => '775',
        'bootstrap/cache/' => '775',
        '.env' => '664',
    ],

    /*
    |--------------------------------------------------------------------------
    | مسیرهای ثابت مایگریشن
    |--------------------------------------------------------------------------
    | علاوه بر database/migrations، هر مسیر دیگری که مایگریشن داری (مثلاً یک
    | پکیج داخلی خاص) رو می‌تونی اینجا اضافه کنی. مسیرها باید absolute باشن.
    */
    'migration_paths' => [
        database_path('migrations'),
    ],

    /*
    |--------------------------------------------------------------------------
    | الگوهای Glob برای پیدا کردن خودکار مسیر مایگریشن ماژول‌ها
    |--------------------------------------------------------------------------
    | اگر پروژه ماژولار است (مثلاً با پکیج nwidart/laravel-modules یا ساختار
    | مشابه)، این الگوها به‌صورت خودکار پوشه‌های مایگریشن هر ماژول را پیدا
    | می‌کنند. چون نصب‌کننده مایگریشن‌ها را تکه‌تکه (فایل به فایل) اجرا می‌کند
    | و مستقیماً وارد Migrator می‌شود، مسیرهایی که ماژول‌ها معمولاً خودشان با
    | loadMigrationsFrom در Service Provider ثبت می‌کنند دیده نمی‌شوند؛ به
    | همین دلیل لازم است اینجا هم صراحتاً معرفی شوند. الگوهای رایج از قبل
    | اضافه شده‌اند؛ در صورت نیاز آن‌ها را متناسب با ساختار پروژه‌ات ویرایش کن.
    */
    'migration_module_globs' => [
        base_path('Modules/*/Database/Migrations'),
        base_path('Modules/*/database/migrations'),
        base_path('app/Modules/*/Database/Migrations'),
        base_path('Modules/*/src/Database/Migrations'),
    ],

    /*
    |--------------------------------------------------------------------------
    | اجرای Seeder به همراه Migration
    |--------------------------------------------------------------------------
    */
    'run_seeders' => true,

      /*
    |--------------------------------------------------------------------------
    | کلاس های Seeder جهت اجرا
    |--------------------------------------------------------------------------
    */
    'seeder_classes' => [
        \Database\Seeders\DatabaseSeeder::class,
    ],
    /*
    |--------------------------------------------------------------------------
    | مدل کاربر برای ساخت ادمین در مرحله آخر
    |--------------------------------------------------------------------------
    */
    'user_model' => \App\Models\User::class,

     /*
    |--------------------------------------------------------------------------
    | نام نقش ادمین در مرحله آخر
    |--------------------------------------------------------------------------
    */
    'admin_role_name' => 'super-admin',

    /*
    |--------------------------------------------------------------------------
    | فیلدهای اضافی هنگام ساخت حساب ادمین
    |--------------------------------------------------------------------------
    | اگر جدول users پروژه‌ات ستون‌های سفارشی NOT NULL دارد (مثلاً role_id،
    | company_id، status و ...) که در فرم ساخت ادمین نصب‌کننده وجود ندارند،
    | مقدار پیش‌فرض آن‌ها را اینجا مشخص کن تا موقع insert با خطای دیتابیس
    | مواجه نشوی. این مقادیر مستقیماً به آرایه create() اضافه می‌شوند و روی
    | فیلدهای name/email/password تأثیری ندارند (آن‌ها همیشه از فرم می‌آیند).
    |
    | مثال:
    | 'admin_extra_fields' => [
    |     'role_id' => 1,
    |     'is_active' => true,
    | ],
    |
    | مقدار می‌تواند closure هم باشد تا در لحظه محاسبه شود:
    | 'admin_extra_fields' => fn () => ['company_id' => \App\Models\Company::first()?->id],
    */
    'admin_extra_fields' => [],

    /*
    |--------------------------------------------------------------------------
    | فیلدهای فرم دیتابیس
    |--------------------------------------------------------------------------
    */
    'database_drivers' => [
        'mysql' => 'MySQL / MariaDB',
        'pgsql' => 'PostgreSQL',
        'sqlite' => 'SQLite',
    ],

];
